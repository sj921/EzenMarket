<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.ezenmarket.user.mapper.UserXmlMapper">

  <insert id="userSignUp" parameterType="com.ezen.ezenmarket.user.dto.User">
    INSERT INTO 
    	userlist(user_number, user_name, user_id, user_pw, nickname, phonenumber, email, user_address) 
    VALUES (
    	user_number_seq.nextval,
    	#{user_name},
    	#{user_id},
    	#{user_pw},
    	#{nickname},
    	#{phoneNumber},
    	#{email},
    	#{user_address}
    )
  </insert>
  
    <select id="idCheck" parameterType="String" resultType="int">
		SELECT 
			COUNT(user_id) 
		FROM 
			userlist 
		WHERE 
			user_id=#{id}
	</select>
	
	<select id="nickCheck" parameterType="String" resultType="int">
		SELECT 
			COUNT(nickname) 
		FROM 
			userlist 
		WHERE 
			nickname=#{nickName}
	</select>
</mapper>